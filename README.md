# madiproj

quicktart:
Lynux/mac:

python3 -m venv venv
. venv/bin/activate
pip install Flask
export FLASK_APP=hello
flask run

Windows:

py -3 -m venv venv
venv\Scripts\activate
pip install Flask
export FLASK_APP=hello
flask run
